import 'package:flutter/material.dart';
import 'register_page.dart';
import 'profile_page.dart';
import 'chat_page.dart';
import 'service_page.dart'; // Example service page
import 'bookings_page.dart';
import 'wallet_page.dart';

// class HomePage extends StatelessWidget {
//   final bool isGuest;

//   const HomePage({super.key, this.isGuest = true}); // Default: Guest Mode

//   void _handleNavigation(BuildContext context, Widget page) {
//     if (isGuest && (page is ProfilePage || page is ChatPage)) {
//       // Redirect guest users to Registration Page
//       Navigator.push(
//         context,
//         MaterialPageRoute(builder: (context) => const RegisterPage()),
//       );
//     } else {
//       // Navigate to the requested page
//       Navigator.push(
//         context,
//         MaterialPageRoute(builder: (context) => page),
//       );
//     }
//   }

//   @override
//   Widget build(BuildContext context) {
//     return Scaffold(
//       appBar: AppBar(title: const Text("Home Page")),
//       body: Column(
//         mainAxisAlignment: MainAxisAlignment.center,
//         children: [
//           // Profile Button - Redirects Guest to Registration
//           ElevatedButton(
//             onPressed: () => _handleNavigation(context, const ProfilePage()),
//             child: const Text("Go to Profile"),
//           ),

//           // Chat Button - Redirects Guest to Registration
//           ElevatedButton(
//             onPressed: () => _handleNavigation(context, const ChatPage()),
//             child: const Text("Go to Chat"),
//           ),

//           // Services (Works Normally)
//           ElevatedButton(
//             onPressed: () => _handleNavigation(context, const ServicePage()),
//             child: const Text("Explore Services"),
//           ),

//           // Bookings (Works Normally)
//           ElevatedButton(
//             onPressed: () => _handleNavigation(context, const BookingsPage()),
//             child: const Text("Your Bookings"),
//           ),

//           // Wallet (Works Normally)
//           ElevatedButton(
//             onPressed: () => _handleNavigation(context, const WalletPage()),
//             child: const Text("Your Wallet"),
//           ),
//         ],
//       ),
//     );
//   }
// }
